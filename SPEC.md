# ゲーム仕様

## 現行 v0.6：おとどけ・横流入・独立アイテム

4個消しの単独効果を、目標優先で飛んで1果物を消す「おとどけ」に変更。通常手がない場合は落下・補充後まで試算して続行可能な着地点を選ぶ。例外時のライン供給、合成維持などの詳細は [飛行アイテム仕様](docs/FLIGHT_UPDATE.md)。

4・5・6個以上で生成する特殊は果物属性を持たず、通常マッチへ参加しない。隣の通常果物との交換だけで発動。壁や障害物との交換は不可。影響と例外の詳細は [独立アイテム設計](docs/ITEM_UPDATE.md)。

## 可変盤面・壁・正方形マッチ
全20面を最大11×9の自由形状に再調整。小さな盤面も使い、面積より構造の複雑さで難易度を作る。定義は `src/levels.js`、落下・壁・補充の詳細は [盤面設計](docs/BOARD_UPDATE.md)。

全レベルで横向き共通UI（左目標・中央盤面・右操作）。正方形セルを維持し盤面の縦横比に合わせてサイズを決める。縦向きは回転案内付き補助表示。回転で進捗は失わない。

地形と壁は果物の移動から独立。保存キー・形式はv1を維持し、クリア済みIDや設定を継続する。水流は未実装。

## 基本
最大11×9の可変外枠（全マス使用は不要）、6種類（りんご・オレンジ・レモン・ぶどう・いちご・キウイ）。上下左右の隣接交換。交換した果物が3個以上の直線または2×2正方形に参加する場合だけ成立。通常の不成立交換は元に戻す。特殊同士、および任意の特殊と通常果物の交換はマッチ不要で成立する。

クリック/タップで2マス選択、スワイプ、キーボード（矢印で移動、Enter/Spaceで選択）。処理中・ダイアログ表示中の盤面入力は禁止。時間/ターン制限なし。リセットはそのレベルのみ最初から。

## マッチと特殊生成
横/縦の3個以上と同色2×2を検出。壁で区切られたセルは連続とみなさず、正方形は4辺に壁がない場合だけ成立。同じ果物のマッチが重なる場合は結合し、重複セルを除いた個数で判定。離れたマッチは別グループ。

| 個数 | 生成 | 単独効果 |
|---|---|---|
| 3 | なし | マッチのみ |
| 4 | おとどけ「➤」 | 飛んで通常果物1個を収穫 |
| 5 | ライン「↔ / ↕」 | 1行 / 1列全体 |
| 6以上 | 虹「✦」 | 交換相手の種類を全盤面から消す |

全特殊は `fruit: null` と特殊種別で表し、空きマスとは区別する。通常果物との交換後のアイテム位置で発動し消滅する。おとどけは着地点の通常果物1個、ラインは交換方向（左右=横、上下=縦）の一列。虹は相手の種類を全収穫。他の特殊に巻き込まれたアイテムも1回追加発動し、ラインは生成時の方向、虹は盤面の最多果物（同数なら一覧順）を対象にする。

生成位置は「交換先 → 交換元 → グループの行優先最小インデックス」。生成時のライン方向は縦に長いグループなら縦、それ以外は横。これは巻き込み発動用の方向で、通常スワップ時は交換方向を優先。連鎖では行優先最小位置。生成セルはその波で消えず、収集数には加算しない。特殊自体の消費は果物収穫数に数えない。新しい特殊は次の波以降に発動できる。既存特殊を含むマッチは既存効果を発動し、新たな特殊は生成しない。

## 特殊合成
交換先を効果の中心とする。交換した2セルは必ず消去対象。効果は壁・無効空間・永久ブロックを越えて届くが、そこに果物を置いたりブロックを消したりしない。

| 組み合わせ | 効果 |
|---|---|
| おとどけ × 十字 | 中心5×5を消去 |
| 十字 × ライン | 中心と隣の3行＋3列を消去 |
| ライン × ライン | 交換した双方の行と列を消去 |
| ライン × 虹 | 最多の通常果物の各位置から、縦横両ラインを発動 |
| 虹 × 虹 | 全盤面に効果。保護されていない果物を全消去 |
| 十字 × 虹 | 最多の通常果物の各位置で3×3を消去 |

巻き込んだ特殊は1回ずつ追加発動。消去対象は集合で管理し二重計上しない。虹×虹でも障害物内の果物の保護ルールを優先し、障害物は1段階だけ破壊。補充は演出の後に行う。

## 障害物
| 障害物 | マッチ/交換 | 破壊 | 落下 |
|---|---|---|---|
| 氷 | 内部果物は対象外 | 隣接果物消去または特殊直撃で1回 | 固定・通過不可 |
| 箱 | 内部果物は対象外 | 1回で凹み、2回で開放 | 固定・通過不可 |
| 埋まりマス | 果物なし | 不可 | 通過不可 |
| 無効空間 | マス自体なし | 対象外 | 停止・通過不可 |
| 壁（マス間） | 交換・通常マッチを遮る | 不可 | 辺の通過不可 |

隣接ダメージは壁に遮られる。特殊の直撃は壁を越えて届く。障害物ダメージは消去の1波につき最大1。直撃と隣接の重複でも1。破壊自体は隣接ダメージを伝播しない。開放された果物はその波では消えず、次の重力とマッチから通常扱い。

重力はまず真下を全体で解決。真下が壁・空間・固定障害物なら左→下、右→下の順で回り込む。中継・行先は空の有効マス、経路の2辺は壁なしが条件。下が通常果物なら滑らない。さらに空洞下の空きへ、上から供給できる列に近い左右の既存果物を流す。距離の小さい側を優先し同距離なら左。壁や無効マスは越えない。静止後に残る空きだけに補充して再落下を繰り返す。回廊では空きが左右の列へ伝わり、上端から補充される。空き数が必ず減るため補充不能にならない。移動は経路どおり描画し、無効空間を通過させない。

## 進行と救済
初期盤面はマッチなし・有効手あり。落下・補充後の自動マッチを連鎖として表示（最初の波=1）。全目標を達成し連鎖が終わった時点でクリア。

通常マッチ手も特殊を交換発動する手もない場合だけ手詰まりとする。交換できない孤立特殊は有効手としない。手詰まり時は障害物と特殊を維持し、開放済み果物の種類を再抽選する（収集には加算しない）。上限到達時は虹を付与。ヒントで合法手を表示でき、ペナルティなし。

1〜2：小さな盤面、3〜：自由形状と壁、4〜5：氷、6〜7：箱、8〜9：回廊・非対称、10〜：最大11×9も含む複合構造。全レベルの障害物数・目標・形状は levels.js。後続レベルも同じ定義を追加する。

## UI・保存
テーマはクリーム色＋深緑＋果物の差し色。自作SVGの形と色で種類を識別。特殊は果物を描かず、おとどけ・四方向ライン・虹の独立した模様で表示。氷は青い表面、箱は茶色、永久ブロックは暗い格子。

消去の縮小・粒子、落下移動、特殊範囲の色付け、虹合成の大きな光輪。動きを減らす設定とOS設定を尊重する。効果音は標準 Web Audio で合成、初期OFF。

localStorage にバージョン、最高到達、クリア済みID、音とモーション設定を保存。進行中の盤面は再読込で再開しない。未開放レベルは選択不可。設定内で確認して全セーブ削除可能。保存不可は画面通知してメモリで継続。

効果音がONのとき、各消去波の開始に `assets/audio/fruit-clear.mp3` を再生する。連鎖で直前の音が残っていても次の消去音を鳴らせるよう4音のプールを循環利用し、連鎖数と特殊発動に応じて音量を小幅に調整する。ブラウザの再生制限や音声非対応時もゲーム進行は止めない。

## v0.7 ストック・盤面調整（現在の仕様）
合計6個、1ステージ3個、初期おとどけ1＋ライン1。初回クリアのみ回収し、使用可能な在庫があれば手詰まり時はプレイヤーの選択を待つ。Lv8・17・18を調整。詳細は [STOCK_UPDATE.md](docs/STOCK_UPDATE.md) と [評価結果](docs/STOCK_BALANCE_REPORT.md) を参照。旧記述と異なる点はこの仕様を優先。
