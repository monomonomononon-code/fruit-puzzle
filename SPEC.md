# ゲーム仕様

## 基本
8×8、6種類（りんご・オレンジ・レモン・ぶどう・いちご・キウイ）。上下左右の隣接交換。交換した果物が3個以上の直線に参加する場合だけ成立。通常の不成立交換は元に戻す。特殊同士、および虹と通常果物の交換は例外として成立する。

クリック/タップで2マス選択、スワイプ、キーボード（矢印で移動、Enter/Spaceで選択）。処理中・ダイアログ表示中の盤面入力は禁止。時間/ターン制限なし。リセットはそのレベルのみ最初から。

## マッチと特殊生成
横/縦の3個以上を検出。同じ果物のマッチ線が交差する場合は結合し、重複セルを除いた個数で判定。離れたマッチは別グループ。

| 個数 | 生成 | 単独効果 |
|---|---|---|
| 3 | なし | マッチのみ |
| 4 | 十字「＋」 | 中心と上下左右1マス |
| 5 | ライン「↔ / ↕」 | 1行 / 1列全体 |
| 6以上 | 虹「✦」 | 交換相手の種類を全盤面から消す |

4/5特殊は元の果物の種類を持つ。通常マッチ参加、または他の特殊の効果に巻き込まれると発動する。虹は通常マッチに参加せず、通常果物と交換すればその種類、効果に巻き込まれた場合は生成時の種類を消す。

生成位置は「交換先 → 交換元 → グループの行優先最小インデックス」。ライン方向は縦に長いグループなら縦、それ以外は横。連鎖では行優先最小位置。生成セルはその波で消えず、収集数には加算しない。新しい特殊は次の波以降に発動できる。既存特殊を含むマッチは既存効果を発動し、新たな特殊は生成しない。

## 特殊合成
交換先を効果の中心とする。交換した2セルは必ず消去対象。効果は永久ブロックを越えて届くが、ブロックそのものは消さない。

| 組み合わせ | 効果 |
|---|---|
| 十字 × 十字 | 中心5×5を消去 |
| 十字 × ライン | 中心と隣の3行＋3列を消去 |
| ライン × ライン | 交換した双方の行と列を消去 |
| ライン × 虹 | ライン果物と同じ種類の各位置から、縦横両ラインを発動 |
| 虹 × 虹 | 全盤面に効果。保護されていない果物を全消去 |
| 十字 × 虹 | 十字果物と同じ種類の各位置で3×3を消去 |

巻き込んだ特殊は1回ずつ追加発動。消去対象は集合で管理し二重計上しない。虹×虹でも障害物内の果物の保護ルールを優先し、障害物は1段階だけ破壊。補充は演出の後に行う。

## 障害物
| 障害物 | マッチ/交換 | 破壊 | 落下 |
|---|---|---|---|
| 氷 | 内部果物は対象外 | 隣接果物消去または特殊直撃で1回 | 固定・通過不可 |
| 箱 | 内部果物は対象外 | 1回で凹み、2回で開放 | 固定・通過不可 |
| 埋まりマス | 果物なし | 不可 | 通過不可 |

障害物ダメージは消去の1波につき最大1。直撃と隣接の重複でも1。破壊自体は隣接ダメージを伝播しない。開放された果物はその波では消えず、次の重力とマッチから通常扱い。

重力は各列の障害物で区切った連続区間内で下向き。区間の上端から補充するため、埋まりマスの下にも果物を供給できる（ブロックを通過する演出は行わない）。

## 進行と救済
初期盤面はマッチなし・有効手あり。落下・補充後の自動マッチを連鎖として表示（最初の波=1）。全目標を達成し連鎖が終わった時点でクリア。

手詰まり時は障害物と特殊を維持し、開放済み果物の種類を再抽選する（収集には加算しない）。上限到達時は虹を付与。ヒントで合法手を表示でき、ペナルティなし。

1〜3：果物目標、4〜5：氷、6〜7：箱、8〜9：埋まりマス、10：複合。全レベルの障害物数・目標・形状は levels.js。後続レベルも同じ定義を追加する。

## UI・保存
テーマはクリーム色＋深緑＋果物の差し色。自作SVGの形と色で種類を識別。特殊は果物の上に識別バッジ。氷は青い表面、箱は茶色、永久ブロックは暗い格子。

消去の縮小・粒子、落下移動、特殊範囲の色付け、虹合成の大きな光輪。動きを減らす設定とOS設定を尊重する。効果音は標準 Web Audio で合成、初期OFF。

localStorage にバージョン、最高到達、クリア済みID、音とモーション設定を保存。進行中の盤面は再読込で再開しない。未開放レベルは選択不可。設定内で確認して全セーブ削除可能。保存不可は画面通知してメモリで継続。
