# 開発ガイド

## プロジェクト
「果実日和 / Fruitful」：時間・ターン制限のない、ブラウザ向け2D 3マッチパズル。日本語 UI、PC・スマホ両対応。

## 作業方針
- Game Studio の設計・UI・プレイテストの指針を使用する。
- 既存ファイルと Git 状態を確認してから編集する。無断で既存データを削除しない。
- 依存追加は必要性を説明する。現時点の実行時依存はゼロ。
- ゲームルールは `src/engine.js`、レベルは `src/levels.js`、描画・入力は `src/app.js` に分離する。
- レベル固有処理をエンジンに埋め込まず、データで追加する。
- 乱数は注入可能にして、重要ルールを Node.js 標準の自動テストで確認する。
- 処理中は入力をロックする。消去対象と障害物ダメージは Set で重複排除する。
- 氷・箱の中身は同じ処理で消去しない。永久ブロックを落下が通過してはいけない。
- スマホのタッチ、キーボード、動きを減らす設定、保存失敗を考慮する。
- 新機能・仕様変更は PLAN.md / SPEC.md / README.md の該当箇所も更新する。

## コマンド
- 起動：`node scripts/serve.mjs` → http://localhost:4173
- ロジック検証：`node --test tests/engine.test.mjs`
- ブラウザ検証：`node scripts/browser-test.mjs`（Playwright が必要。README参照）
- ビルド不要。GitHub Pages はルートの index.html から相対パスで動かす。

## 完了条件
ルールの自動テスト、実ブラウザでの操作、PC・スマホのスクリーンショットを確認する。未検証事項は検証済みと書かない。GitHub 公開をしていなければ明記する。
