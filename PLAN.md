# 果実日和 — 制作計画

## 1. 開発環境確認（2026-09-15）
- 作業先：`C:\AI\広告アプリ\パズルゲー`。開始時は空フォルダ。
- Git 実行ファイルあり。既存リポジトリ・remote なし。既存ファイルの移行は不要。
- Node.js v24.19.0 利用可。npm / python は通常の PATH にはない。
- Codex 同梱環境に Playwright、Python、pnpm があり、Chrome / Edge も存在する。
- ゲーム実行とロジックテストに追加インストールは不要。検証には同梱 Playwright を利用する。

## 2. 技術構成と理由
HTML + CSS + JavaScript ES Modules、DOM の8×8盤面、Web Animations API。Node.js 標準 HTTP サーバーと node:test を開発用に使用。

Game Studio の通常推奨は Phaser。今回は物理演算・カメラ・大量スプライトが不要な64マスのパズルなので、ユーザーが許可した素のWeb構成を選ぶ。依存インストールとビルドを省け、ボタンのアクセシビリティとスマホ対応を保ちやすく、GitHub Pagesへそのまま配置できる。後から描画を Phaser に置換できるようにルールを描画から分離する。

果物は同梱の自作 SVG。外部画像・CDN・フォント・外部サービスに依存しない。Figma / Canva / Mobbin は今回の動く最小版に必須ではないため使用しない。

参考：
- [Web Animations API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API)
- [localStorage](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)

## 3. 作業順序
- [x] 環境確認
- [x] 技術構成決定
- [x] AGENTS.md 作成
- [x] PLAN.md 作成
- [x] SPEC.md に曖昧なルールを確定
- [x] ロジック：スワップ、消去、重力、連鎖、特殊、障害物、シャッフル
- [x] データ：10レベル、拡張時のレベル検証
- [x] UI：果物、アニメーション、目標、選択、設定、リセット、保存
- [x] 自動テストと実ブラウザ確認、問題修正
- [x] README と検証記録、起動したゲームを提示

## 4. MVP の範囲
6果物、10レベル、3種類の特殊（ラインは縦横）、指定5通りを含む特殊合成、氷・箱・永久ブロック、無制限プレイ、目標進捗、連鎖表示、ヒント、PC/スマホ入力、クリアと次へ、レベル選択、音・動きを減らす設定、ローカル保存。

50レベルの制作、広告・課金、アカウント、サーバー保存は対象外。途中盤面の保存は行わず、最高到達・クリア済み・設定を保存する。

## 5. 設計上の判断
- 盤面は8×8。障害物も固定セル。氷・箱・ブロックで区切られた各縦区間の上端から補充。
- 初期マッチをなくし、有効手を保証。手詰まり時は保護されていない果物を再配置。
- 特殊生成は同じ果物の交差するマッチを一群にまとめ、その実マッチ個数で判定。
- 生成位置・特殊組合せ・障害物との相互作用は SPEC.md に表で定義。
- 障害物の周囲に消去可能な領域が残る盤面にし、特殊効果でも障害物を破壊可能にする。
- 再配置が規定回数失敗した場合は、通常果物1個を虹特殊に変えて有効手を保証。目標進捗は変えない。
- 連鎖が安全上限に達した場合は安定盤面へ再配置し、処理を終了させる。
- 保存内容は型・範囲を検証し、破損や保存不可でもゲームを継続できるようにする。

## 6. 検証計画
- 固定盤面：3/4/5/6個以上とT/L字、無効スワップ復元、生成位置。
- 特殊：4×4、4×5、5×5、5×6、6×6、通常果物と虹。
- 障害物：隣接ダメージ1回/波、箱2回、開放直後の果物保護、埋まりマスを越えない落下。
- シード付き試行：10レベルの初期盤面、有効手、連鎖の安定化、目標全達成、手詰まり救済。
- 実ブラウザ：起動、クリック・スワイプ、入力ロック、メニュー、設定、クリア、保存再読込、画面幅。
- PC・スマホのスクリーンショットとコンソールエラー確認。

## 7. GitHub
初回制作時はローカル成果物の完成までを実施。その後の依頼で `monomonomonomonomononon-code/fruit-puzzle` を反映先に指定。GitHub連携で空リポジトリ（ブランチ・コミットなし）を確認してから、動作確認済みファイルを登録する。既存ファイルの削除やゲームロジックの変更は行わない。

反映作業ではREADMEと.gitignoreを整備し、反映後にロジック・ブラウザテストを再実行する。GitHub Pagesの公開設定変更は行わず、静的構成とサブディレクトリでの動作を確認する。
