# 検証結果

## v0.6 4マッチおとどけ（2026-09-15）

- 既存78件成功＋飛行専用5件成功、計83件（分割実行）。旧十字単独範囲の期待値を1果物飛行へ変更。長連鎖保護のテストは先読み対象外のラインで検証。
- 通常手ゼロの固定盤面20シードで、飛行後の通常手・安定盤面・シャッフルなしを確認。未達成目標優先、1果物収穫、試算と採用結果の再現性、2セルしかない例外でのライン供給を確認。
- 全20面×3シードと後半10面×30シードの全目標達成が成功。5/6の単独効果、全合成、保存、横流入の回帰を確認。
- ChromeでPC/スマホの飛行中画像、入力ロック、着地消去、通常手への復帰を確認。通常操作・独立アイテム・横流入のブラウザテストも成功。
- 証跡：`test-results/flight/`。PCとスマホの画像を実際に開いて確認。通常操作のJavaScript例外・コンソールエラー0件。
- 先読み保証は通常果物との直接交換で使用するおとどけが対象。合成や他の特殊に巻き込まれた途中発動は既存の全手終了時救済を使う。探索上限内で通常手を見つけられない場合の例外は画面でライン供給を通知。
- ローカル更新のみ。GitHub・Pages・Vercelの再公開は未実施。実機Safariは未検証。



## v0.5 空洞下への横流入（2026-09-15）

- Lv8中央下の消去後、既存果物が左右から流入し、新規補充の発生位置が上端だけになることをID・移動経路で確認。
- 特殊の種類/ID保持、壁・無効空間の通過禁止、孤立区間の局所補充、全20面の完全消去後の停止・充填を追加検証。
- 既存74件を実行し、横流入が可能になった1件の旧「その場に留まる」期待値を変更。変更後の盤面13件と追加4件を再実行し17件成功。合計78件の最終期待値で成功を確認（分割実行）。
- 全20面×3シードと後半10面×30シードの計360回の全目標達成も成功。エンジン・アイテムの他61件は変更なく成功。
- ChromeでLv8中央下の特殊交換を実クリックし、横流入経路、上端補充、アニメーション完了、最終描画とエンジンの一致を確認。PC/スマホ画像を開いて確認。証跡は `test-results/flow/`。
- 通常ブラウザ操作と独立アイテムのブラウザ操作テストも成功。保存・レベル目標・デザインは変更なし。実機Safari未検証。GitHub/Pages/Vercel反映は未実施。



## v0.4 独立アイテム（2026-09-15）

- ロジックテスト74件成功、失敗0件。生成、果物属性なし、通常マッチ除外、上下左右の交換発動、ライン方向、アイテムの収穫数除外、全6種合成、最多果物の対象選択を検証。
- 通常手ゼロ＋使用可能な特殊はシャッフルなし。壁で孤立した特殊は有効手に数えない。特殊を空白扱いせず、回り込み落下で種類・IDを保持。
- 全20面×3シードとLv11〜20×30シードの計360試行で全目標クリア。初期生成は全20面×30シードで検証。今回、レベル配置・目標・保存形式・果物SVGは変更していない。
- Chromeの既存3スクリプトと `scripts/items-browser-test.mjs` が成功。通常手ゼロの盤面でヒント→実クリック→特殊発動→表示同期、横/縦ライン、アイテムの表示と操作可能状態を確認。
- PC・スマホ横向きの独立アイテム画像を開いて確認。証跡は `test-results/items/`。既存の横向き・縦向き・タッチ・回転・保存・クリア遷移の確認も成功。
- 60連鎖を越える異常な計算はシャッフルせず操作と進捗を巻き戻すテストを追加。通常プレイの360試行では有効手処理に失敗なし。
- シャッフル頻度の旧版との比較や実プレイヤーによる難易度評価は未実施。特殊を使用できる局面では自動救済しない動作を検証した。実機Safariは未検証。
- ローカル実装・検証まで完了。GitHub反映・Pages/Vercelの再公開は未実施。



## v0.3 可変盤面・壁・2×2（2026-09-15）

- ロジックテスト **51件成功、失敗0件**。旧仕様の64マス固定・Lv1〜10配置不変・縦落下限定の期待値を今回の依頼に合わせて更新。既存の特殊・全6種合成・氷/箱・連鎖・保存・クリア判定の回帰は維持。
- 全20面×30シードで初期マッチなし・合法手あり。全20面×3シード（60試行）とLv11〜20×30シード（300試行）を合法手だけで全目標達成まで確認。
- 最大11×9、無効空間、壁、2×2と直線の結合、11列での縦特殊判定、落下優先、左/右回り込み、角の通過禁止、分断区間の補充、ID重複なし、手詰まり救済を確認。
- Chrome操作スクリプト3本とも成功。全20面×3サイズ（844×390、568×320、1366×900）でセル形状・地形表示・画面内表示を確認。横持ち5サイズ×5面で目標・盤面・操作・連鎖の重なりなし。
- 実クリックで2×2特殊生成、壁を挟む交換の拒否、回り込み落下のアニメーション、実タッチイベントでスワイプ、二重入力禁止、回転・保存・クリア画面・次面への遷移を確認。
- PC、スマホ横向き（Lv1、Lv10、Lv20、最小568×320）、縦向きのスクリーンショットを開いて確認。JavaScript例外・通常操作スクリプトのコンソールエラー0件。
- 証跡：`test-results/board/`、`test-results/landscape/`、`test-results/browser-report.json`（Git対象外）。
- Node.jsと既存の同梱Playwright/Chromeを使用。依存追加なし。ゲーム素材と保存形式は維持。
- 今回はローカル更新のみ。GitHubへのコミット・反映、Pages/Vercelの再公開は未実施。実機Safariは未検証。複数乱数でのクリア検証であり、全乱数系列の有限手クリアを数学的に保証するものではない。

以下は過去バージョンの検証履歴。


## v0.2 横向き・20レベル更新

- 既存31テストを維持し、構造検査・旧データ一致・保存互換などを追加した38件のロジックテストが成功。
- Lv1〜10は更新前JSONと完全一致。ゲームエンジン、保存モジュール、果物素材は変更なし。
- 全20レベル×30シードの初期盤面検証。追加10レベル×30シード、計300試行を合法手だけで全目標達成まで確認。
- 横向き：844×390 / 667×375 / 568×320 / 932×430 / 740×300。Lv1・10・12・17・20で盤面が画面内に収まり、左右の操作・連鎖表示と重ならないことを確認。
- 分断盤面での実タッチスワイプ、縦横回転による盤面・進捗保持、旧セーブからLv11への継続、Lv20最終表示、メニュー、保存再読み込みを確認。
- `scripts/browser-test.mjs` による通常のPC・縦向きスマホ操作も成功。
- 画像・レポートは `test-results/landscape/`。公開サイトのデータや進行状況を変更する操作は行っていない。
- スマホ検証はChromeのタッチエミュレーション。実機Safari・極端に小さい表示領域・全乱数系列の有限手クリア保証は検証範囲外。

以下は最小完成版時点の検証履歴。

実施日：2026-09-15（日本時間）。Node.js v24.19.0、Chrome 153.0.8010.36、同梱Playwrightを使用。

## 結果

- `node --test tests/engine.test.mjs`：31件すべて成功。
- `node scripts/browser-test.mjs`：PC・スマホ操作、画面遷移、保存、表示幅の確認に成功。
- ブラウザのJavaScript例外・コンソールエラー：0件。
- PC、スマホ、Lv10の画像を実際に開いて確認。果物の形状、目標、氷・箱・ブロックを判別でき、要素の重なりなし。

## 重点項目

| 項目 | 方法・結果 |
|---|---|
| 初期盤面 | 10レベル×30シード。即時マッチなし・合法手あり |
| 特殊生成 | 3/4/5/6/7個、T字・L字、縦ライン、交換先優先、連鎖生成位置 |
| 特殊合成 | 指定5種類＋十字×虹をロジックで検証。指定5種類は実ブラウザでも解決 |
| 二重消去 | 対象集合、収集数、巻き込まれた特殊の1回発動を検証 |
| 連鎖 | 複数波、連鎖番号、終了後の安定盤面を検証 |
| 氷・箱 | 直消し不可、隣接と直撃の重複排除、箱2回、開放直後の果物保護 |
| 永久ブロック | 果物なし、重力が通過しない、区間内の順序を維持 |
| 手詰まり | 実際の合法手ゼロ盤面からの再配置、処理後の自動検出、虹救済 |
| クリア判定 | 全目標AND条件、10レベル×3シードで合法手による自動プレイ全クリア |
| 入力 | PCクリック、Chromeの実タッチイベントによるスワイプ、処理中の入力抑止 |
| 保存 | 最高到達・クリア済み・設定の再読み込み、型と範囲、壊れたJSONから起動 |
| 画面 | レベル選択、未開放レベル、ヘルプ、設定、リセット、クリア、次へ |
| レスポンシブ | 320 / 360 / 390 / 640 / 768 / 1024 / 1366px。横あふれなし |

## 生成物

`test-results/desktop.png`、`mobile.png`、`level10-desktop.png`、`level10-mobile.png`、`rainbow-effect.png`、`browser-report.json`。

画像は最新のテスト実行で上書きします。通常プレイとは独立したブラウザコンテキストを使用しました。

## 制約・未検証

- スマホはChromeのタッチ・画面エミュレーション。iPhone Safari / Android実機、Firefoxでは未検証。
- 低スペック端末のFPS測定、長時間放置、全乱数系列の到達性を証明する検証は未実施。粒子数と連鎖回数に上限を設けています。
- 自動プレイのクリアは実施したシード範囲での結果です。人間の遊びやすさや難易度曲線は追加プレイで調整できます。
- 音の出力処理は実装済みですが、聴感での音量・音色評価は未実施。
- GitHub PagesとVercelの公開URLで、更新版の画面表示、JavaScript、MP3音源のHTTP配信を確認しました。
- 進行中の盤面の復元、クラウド保存、広告、課金は対象外です。

## v0.7 ストック・盤面調整（2026-09-15）
- 既存ルール83件成功。最終Lv18配置でengine/boardを再検証し、新規stock10件も成功（計93件の異なるテスト）。
- Chrome実操作：基本操作、合成、左右流入、飛行、保存、ストック選択/取消/確定、初回クリア表示。Lv8・17・18各最大15手。
- 横向き5画面サイズ×5面で盤面・目標・操作・連鎖の重なりなし。追加ボタンの重なりを修正後、再検証成功。タッチはChromeの入力エミュレーションで確認。
- PC/スマホ画像はtest-results/stock、横向き比較画像はtest-results/landscape。保存失敗・破損セーブの既存確認も成功。
- 大量比較の数値・限界はdocs/STOCK_BALANCE_REPORT.md。通常の消去・落下エンジンはv0.6とファイル内容一致。
- v0.7一式をGitHubへ反映し、GitHub PagesとVercelで公開済みです。

追加のクリア通し確認：ChromeでLv8=32操作/ストック0/シャッフル0、Lv17=71/3/1、Lv18=57/0/5。各1回の実クリック確認。ストック使用上限と次面への消費引継ぎを確認。4条件×20面×100回=8,000試行はすべてクリア（打切りなし）。

## v0.7 消去音（2026-09-16）
- ユーザー提供MP3と配信用コピーのSHA-256一致：`FDB46EECB1FBF7951AAE18B3E2507CDFC1402DD634B4484F864C224989AD33AB`。
- Chromeで `assets/audio/fruit-clear.mp3` がHTTP 200・`audio/mpeg`として読み込まれることを確認。
- 実際の有効スワップから消去音の再生を確認。音量上限52%、効果音OFFでは再生0回。
- ルールテスト93件成功。横向き5サイズ×5レベル、タッチ、回転、保存、Lv10→11、Lv20表示を再確認。
- GitHub PagesとVercelから取得した音源は各10,946バイトで、元ファイルとSHA-256が一致。
